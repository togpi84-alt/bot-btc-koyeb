name: Keep Alive - Crypto Alert Bot

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Ping Crypto Alert Bot
        run: |
          curl -f https://crypto-alert-bot--togpi84.replit.app || true
      - name: Log Success
        run: echo "✅ Bot ping successful"
